server.port=8080
spring.application.name=mvax-mw-tools

# CORS - allow Angular dev server
app.cors.allowed-origins=http://localhost:4200,http://localhost:80,http://frontend:80

# SSE timeout for long-running pipeline operations (2 hours)
spring.mvc.async.request-timeout=7200000
